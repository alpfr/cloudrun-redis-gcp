apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: selfsigned-issuer
  namespace: default
spec:
  selfSigned: {}
---
# Alternative: Let's Encrypt ClusterIssuer (Uncomment if exposing publicly with a DNS record)
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: letsencrypt-prod
# spec:
#   acme:
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: your-email@example.com # Replace with your email
#     privateKeySecretRef:
#       name: letsencrypt-prod-private-key
#     solvers:
#       - http01:
#           ingress:
#             class: istio
